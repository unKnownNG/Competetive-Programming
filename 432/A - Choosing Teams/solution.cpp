#include <bits/stdc++.h>using namespace std; void solve(){    // LOGIC    int n, k;    cin >> n >>k;     int c = 0;    for(int i = 0; i < n; i++){        int a;        cin >> a;        if(a <= (5 - k)) c++;    }     cout << c / 3;} int main(){    ios::sync_with_stdio(false); // Fast I/O    cin.tie(nullptr);            // Untie cin from cout  solve();}