#include <bits/stdc++.h>using namespace std; #define ll long long void solve(){    // LOGIC    ll n, ma = 0;    cin >> n;    vector<ll> v(n);     vector<ll> ev;    for(auto &i : v) {cin >> i; if(i %2 == 0) {ev.push_back(i); ma = max(ma, i);}}      // if(ev.size() >= 2){    //     cout << ev[0] << " " << ev[1] << endl;    //     return;    // }     // sort(v.begin(), v.end());     for(int i = 0;i < n; i++){         for(int j = 0; j < i ; j++){            if((v[i] % v[j]) % 2 == 0){                cout << v[j] << " " << v[i] << endl;                return;            }        }    }     cout << "-1\n";    } int main(){    ios::sync_with_stdio(false); // Fast I/O    cin.tie(nullptr);            // Untie cin from cout     int n;    cin >> n;    while(n--) solve();}