#include <bits/stdc++.h>using namespace std; void solve(){    // LOGIC    int n, k;    cin >> n >> k;    string s;    cin>> s;     int c = 0;    bool p = false;     // if(s.substr(0, k - 1).find('1') != string::npos){    //     c++;    //     p = true;    // }     // for(int i = k - 1; i < n ;i+= k - 1){    //     string t = s.substr(i, k - 1);    //     if(t.find('1') != string::npos){    //         if(p == false) {c++;p = true;}     //     }    //     else{    //         p = false;    //     }    // }    int last = -1e5;    for(int i = 0; i< n; i++){        if(s[i] == '1'){            if(i + 1 - last >= k){                c++;            }            last = i + 1;         }    }    cout<< c << endl;} int main(){    ios::sync_with_stdio(false); // Fast I/O    cin.tie(nullptr);            // Untie cin from cout     int n;    cin >> n;    while(n--) solve();}