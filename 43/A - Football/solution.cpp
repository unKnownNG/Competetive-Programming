#include <bits/stdc++.h>using namespace std; int main(){    // LOGIC    int n;    cin >> n;    int a =0, b =0;    string f, s;    for(int i =0; i < n; i++){        if(i == 0) cin >> f;        else{            string now;            cin >> now;            if(now == f) a++;            else {                b++;                s = now;            }        }    }    cout <<( (a < b) ? s : f);} // int main(){//     ios::sync_with_stdio(false); // Fast I/O//     cin.tie(nullptr);            // Untie cin from cout //     int n;//     cin >> n;//     while(n--) solve();// }