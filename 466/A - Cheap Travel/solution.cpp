#include <bits/stdc++.h>using namespace std; void solve(){    // LOGIC    int a, b,c,d, n, m;    cin >> n  >> m >> a >> b;     // 1-> a ==> 1 = a    // m ->b ==> 1 = (b/m) === a = (b/m) --> a *m = b     int x = a;    int y = (b/m);    if(a *m <= b){        cout << n * a<< endl;    }    else{        cout << (n/ m) * b + min(b, (n % m) * a)<< endl;    }  } int main(){    ios::sync_with_stdio(false); // Fast I/O    cin.tie(nullptr);            // Untie cin from cout solve();}