#include <bits/stdc++.h>using namespace std; bool pali(string& s) {    int i = 0, j = s.size();    if (j == 0) return true;               j--;                                   while (i < j) {        if (s[i] != s[j]) return false;        ++i; --j;    }    return true;} void solve(){    // LOGIC    int n;    cin >> n;    string s;    cin >> s;     int c = 0;    for(int i = 0;i < n; i++) if(s[i] == '1') c++;     cout << c << endl;     for(int i = 0; i< n; i++){        if(s[i] == '1'){            cout << i + 1 << " ";        }    }    cout << endl;} int main(){    ios::sync_with_stdio(false); // Fast I/O    cin.tie(nullptr);            // Untie cin from cout     int n;    cin >> n;    while(n--) solve();}