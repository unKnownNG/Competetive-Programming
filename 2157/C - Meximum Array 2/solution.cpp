#include <bits/stdc++.h>using namespace std; #define ll long long   void solve(){    ll n, k, q;    cin >> n >> k >> q;            ll small = INT_MAX, big = INT_MIN;    vector<ll>  v(n, -1), ans(n, 0);        while(q--){        ll a,b,c;        cin >> a >> b >> c;        for(int i = b; i <= c; i++){            if(a == 1){                if(v[i-1] == -1) v[i - 1] = 1;                else if(v[i- 1] == 2) v[i-1] = 3;            }            else{                 if(v[i - 1] == -1)                     v[i- 1] = 2;                else if(v[i-1] == 1)                    v[i - 1] = 3;            }        }    }        for(int i = 0; i < n; i++){        if(v[i] == 3) ans[i] = k + 1;        else if(v[i] == 2) ans[i] = i % k;        else ans[i] = k;    }            for(auto i : ans) cout << i << " ";        cout << endl;} int main() {	// your code goes here	int t;	cin >> t;		while(t--) solve();	 }