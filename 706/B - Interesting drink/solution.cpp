#include <bits/stdc++.h>using namespace std; void solve(){    // LOGIC    int n;     cin >> n;        vector<int> v(n);    for(int i = 0; i< n; i++){        cin >> v[i];    }    sort(v.begin(), v.end());        int k; cin >> k;    for(int i = 0; i < k; i++){        int t;        cin >> t;        int c = 0;        auto m = upper_bound(v.begin(), v.end(), t);                 cout << m - v.begin() << endl;    }} int main(){    ios::sync_with_stdio(false); // Fast I/O    cin.tie(nullptr);            // Untie cin from cout    solve();}