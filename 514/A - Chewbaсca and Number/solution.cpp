#include <bits/stdc++.h>using namespace std; void solve(){    string s;    cin >> s; // input number as string     int l = s.size();    char m = '9';     for(int i = 0; i < l ; i++){        if(s[i] == '9' && i == 0) cout << s[i];        else if(s[i] >= '5'){            cout << '9' - s[i];        }        else cout << s[i];    }     // string ans(l, m); // repeat character m, l times    cout << endl;} int main(){    ios::sync_with_stdio(false);    cin.tie(nullptr);     solve();}