#include <bits/stdc++.h>using namespace std; void solve(){    // LOGIC    int n;    cin >> n;    int c = 0, w = n, l = 0;      // while(true){    //     if(l == 1 && w == 1) break;    //     int a  = w - (w / 2) ;    //     w = (w / 2) + (w % 2);    //     l -= (l / 2) ;    //     l += a;    //     c++;    //     cout << w << "w l" << l << endl;    // }    while(w!= 1){        c+= (w/2);        l += (w/2);        w = (w / 2) + (w % 2);        // cout << w << "w ";     }    // cout<< "L "<< l;    while(l != 1){        c+=(l/2);        l -= (l/2);        // cout << l << "l ";     }     cout << c + 1<< endl; } int main(){    ios::sync_with_stdio(false); // Fast I/O    cin.tie(nullptr);            // Untie cin from cout     int n;    cin >> n;    while(n--) solve();}