#include <bits/stdc++.h>using namespace std; void solve(){    // LOGIC    int n;    cin >> n;    int x = 0;     for(int i = 0; i < n; i++){        int a, b,c ;        cin >> a >> b >> c;        int y = a + b + c;        if( y >= 2) x++;    }    cout << x << endl;} int main(){    ios::sync_with_stdio(false); // Fast I/O    cin.tie(nullptr);            // Untie cin from coutsolve();}