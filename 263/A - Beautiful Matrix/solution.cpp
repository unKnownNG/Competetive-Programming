#include <bits/stdc++.h>using namespace std; void solve(){    // LOGIC     vector<vector<int>> v(5, vector<int>(5, 0));    int a = 0, b = 0;      for(int i = 0; i < 5; i++){        for(int j = 0; j < 5; j++){            // int a;            cin >> v[i][j];            if(v[i][j] == 1){                a = i; b = j;                // cout << "a " << a << b <<endl;            }        }    }      cout << abs(a - 2) + abs(b - 2) << endl;} int main(){    ios::sync_with_stdio(false); // Fast I/O    cin.tie(nullptr);            // Untie cin from cout solve();}