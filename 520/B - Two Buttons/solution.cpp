#include <bits/stdc++.h>using namespace std; void solve(){    // LOGIC    int m, n;    cin >> m >> n;    // int a = max(m, n);    // int b = min(m, n);    int a = m,b = n;     int c = 0;    if(a < b){        while (a != b)        {            if(b/2 >= a - (b / 2) && ((b&1) == 0)) b/=2;            else b++;            c++;        }        cout << c;    }    else{        cout << a - b;    }     } int main(){    ios::sync_with_stdio(false); // Fast I/O    cin.tie(nullptr);            // Untie cin from cout solve();}