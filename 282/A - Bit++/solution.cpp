#include <bits/stdc++.h>using namespace std; void solve(){    // LOGIC    int n; cin >> n;    int x = 0;     for(int i = 0; i< n; i++){        string  s;        cin >> s;        if(s[0] == '+' || s[s.size() - 1] == '+') x++;        else x--;    }    cout << x << endl;} int main(){    ios::sync_with_stdio(false); // Fast I/O    cin.tie(nullptr);            // Untie cin from cout  solve();}