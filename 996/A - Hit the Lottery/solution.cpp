#include <bits/stdc++.h>using namespace std; void solve(){    // LOGIC    long long  n;    cin >> n;     long long c = 0;     vector<int> v = {100, 20, 10, 5, 1};      for(int i = 0; i < 5; i++){        if(n >= v[i]){            c += (n /v[i] * 1LL);            n -= (n/v[i] * 1ll)* 1LL * v[i];        }    }     cout << c << endl; } int main(){    ios::sync_with_stdio(false); // Fast I/O    cin.tie(nullptr);            // Untie cin from coutsolve();}