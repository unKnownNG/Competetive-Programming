#include <bits/stdc++.h>using namespace std; void solve(){    // LOGIC    int n, r, y;    cin >> n >> y >> r;    int x = n;     int a = y / 2;    int b = a + r;     // n -= r;    // n -= a;     if( b > n) b = n;     cout << b<< endl;} int main(){    ios::sync_with_stdio(false); // Fast I/O    cin.tie(nullptr);            // Untie cin from cout     int n;    cin >> n;    while(n--) solve();}